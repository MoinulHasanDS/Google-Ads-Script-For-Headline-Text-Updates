# Google Ads Responsive Search Ad Bulk Text Update Script

This Google Ads Script automatically updates **Responsive Search Ad (RSA) headlines** by replacing **any word or phrase** (e.g. `Apply Now → Enroll Today` or `2025 → 2026`) across all **enabled RSAs**, while preserving pinned headlines and logging all changes in a Google Sheet.

---

## 🚀 Key Features

- Replace **any text, word, or phrase** in RSA headlines
- Supports common use cases like:
  - Year updates (`2025 → 2026`)
  - CTA changes (`Apply Now → Enroll Today`)
  - Offer or compliance text updates
- Works only on **ENABLED Responsive Search Ads**
- Preserves **pinned headline positions**
- Safely rebuilds RSAs after modification
- Logs all updates in a Google Spreadsheet
- Skips ads where no matching text is found

---

## 📋 Use Cases

Perfect for advertisers and agencies who need to:

- Perform **bulk text changes** across RSA headlines
- Update year-based messaging at scale
- Change CTAs, offers, or brand wording quickly
- Avoid manual edits across multiple campaigns
- Maintain a **clear audit log** of all changes
